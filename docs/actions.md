# Actions

Actions can run against one row, many rows, or a resource. They support confirmation, danger styling, reason capture, validation, permission checks, async results, toast feedback, and audit logging.

Examples include blocking a user, resetting a password, marking an invoice as paid, refunding an invoice, assigning a lead, sending SMS, exporting selected records, and archiving records.
