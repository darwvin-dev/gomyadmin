# Getting Started

Run the PostgreSQL-backed CRM demo first:

```sh
make demo
```

Then open http://localhost:3000/admin and sign in with `admin@example.com` / `password`.

The demo includes users, customers, invoices, invoice items, payments, tickets, audit logs, files, custom actions, tenant-aware records, and server-side data-grid operations.
