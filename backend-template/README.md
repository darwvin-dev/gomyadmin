# Backend Template

The production backend template lives in `templates/backend-go`.

Generated apps receive a copy tailored to their module name. The template includes a `net/http` compatible server, chi routing, session auth, resource APIs, custom actions, audit events, file upload, and migration files.
