# Internal Templates

The CLI generator currently keeps small bootstrap templates in Go strings so `go install` works without external assets. Larger reusable app templates live in the top-level `templates` directory.
