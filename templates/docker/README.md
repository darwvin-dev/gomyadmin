# Docker Templates

Root-level Docker Compose and service Dockerfiles are currently maintained in `docker-compose.yml`, `templates/backend-go/Dockerfile`, and `templates/frontend-next-shadcn/Dockerfile`.
